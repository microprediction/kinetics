# fastrace has moved

The canonical fastrace crate now lives in the `winning` package repository
(rust/fastrace), per the 2026-08-15 consolidation. The copy in this
directory is retained for reproducibility of the pinned paper tags
(paper-r8 through paper-r11 built from here); new development happens in
winning.
